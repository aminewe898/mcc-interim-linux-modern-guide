# Configurations

This folder contains emulator configuration files used to boot and install MCC Interim Linux.

Main contents:

* Bochs configuration files
* Hardware emulation settings
* Disk image mappings

These configurations were tested on modern Windows systems using Bochs 3.0.
